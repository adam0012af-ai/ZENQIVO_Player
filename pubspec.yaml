name: zenqivo_player
description: ZENQIVO Player - premium media player shell.
publish_to: 'none'
version: 0.14.0+14

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.5.5
  crypto: ^3.0.7
  xml: ^6.6.1
  media_kit_libs_video: ^1.0.7
  media_kit_video: ^2.0.1
  media_kit: ^1.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/branding/
